# AGENTS.md

## 1. 项目范围

- 仓库根目录固定为 `E:\myfund11111`。
- 本地 Codex 工作区只保存源码、文档、测试夹具和不含秘密的配置模板。
- MySQL、真实持仓、上传文件、运行日志、`.env`、API Key 和邮件凭据只保存在服务器。
- 当前过渡期以服务器运行工作区为代码行为基线，本地只做开发镜像；审查后的代码必须再固化到 GitHub 提交，作为后续正式发布依据。

## 2. 代码与文档规范

- 使用中文编写注释和文档，每个函数前写一条简短中文注释说明用途。
- 代码优先可读性，避免硬编码本机或服务器绝对路径。
- `fund-advisor` 引用 `fund-analyzer` 时使用相对定位、环境变量或正式包安装方式。
- 本地测试只使用虚构数据和仓库内测试夹具，不下载服务器真实数据。
- 修改架构、部署、数据库结构或环境变量时，同步更新 `docs/operations/`。

## 3. Git 与发布规范

- 开始工作前检查 `git status`，不得覆盖或清理不属于当前任务的改动。
- 提交前执行与改动相称的测试、构建和敏感信息检查。
- 发布必须使用明确的 Git 提交 SHA，不以未经确认的工作区状态作为发布源。
- 服务器仓库存在未提交改动时停止部署，先确认改动来源。
- 不使用 `git reset --hard`、强制推送或覆盖式文件同步作为日常部署手段。

## 4. 数据与秘密边界

- 禁止把服务器 `.env`、数据库导出、真实持仓、账户数据、私钥或日志全文复制到本地仓库。
- 默认只查询服务状态、表结构、行数、聚合结果和脱敏后的必要样本。
- 不在命令输出、提交、Issue、文档或聊天中展示密码、Token、邮箱、服务器私密地址和完整真实数据。
- 数据库备份在服务器完成，备份文件仍留在服务器受控目录。
- 需要分析真实数据时，优先在服务器运行脚本，仅返回最小化统计结果。

## 5. 服务器操作规则

- 首次接入先核对 SSH 主机指纹，再执行只读巡检。
- 未明确进入部署或维护流程时，服务器操作默认为只读。
- 数据库写入、迁移、服务重启、配置变更、删除和回滚前必须确认目标与恢复方案。
- 数据库迁移前创建服务器端备份；部署后检查后端健康接口、前端页面和关键日志。
- 发现异常时先停止扩大变更范围，记录当前提交和服务状态，再按运维手册回滚。

## 6. 运维入口

- 总体架构：`docs/operations/SYSTEM_ARCHITECTURE.md`
- 日常与故障手册：`docs/operations/RUNBOOK.md`
- SSH 运维工具：`ops/README.md`
- 服务器本地配置：`ops/.env`，该文件被 Git 忽略，禁止提交。
