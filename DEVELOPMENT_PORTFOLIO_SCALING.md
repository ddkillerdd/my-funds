# My Funds 持仓扩展与多基金兼容要求

> 编写日期：2026-08-28
>
> 本文补充 `DEVELOPMENT_PLAN_PROFITABILITY.md` 和 `DEVELOPMENT_DECISIONS.md`。当前约 1.48 元、单只基金仅代表当前生产数据状态，不是产品规模上限，也不得成为代码中的固定假设。

## 1. 结论

用户后续新增基金或追加资金不应要求重构系统。开发必须从第一天同时兼容：

- 空组合。
- 单基金组合。
- 多基金组合。
- 中途新增基金。
- 中途追加或取出现金。
- 部分执行建议。
- 用户进行非建议触发的手工交易。

当前单基金小额持仓不会阻碍 P0 正确性修复，但不能依靠这一个真实样本验证相关性、分散化、再平衡和多基金资金分配。上述功能必须使用本地合成数据和 staging 测试数据验证。

## 2. 禁止硬编码

代码、配置、测试和页面中禁止把以下当前值写成固定逻辑：

- 基金数量为 1。
- 当前基金代码。
- 当前市值为 1.48 元。
- 当前可用资金为 10 元。
- 当前基金类型。
- 当前平台。
- 当前业绩比较基准。

允许在回归测试夹具中使用 1.48 元和 10 元复现小额账户，但夹具名称应明确表明它是测试场景。

## 3. 数据模型要求

### 3.1 组合和持仓

持仓应以多行记录表达，每只基金至少具有独立的：

- 基金代码和名称。
- 平台或账户。
- 已确认份额。
- 待确认申购份额。
- 待确认赎回份额。
- 成本和累计费用。
- 最新可用净值及日期。
- 基准代码和基金分类。

现金应作为组合级资产独立记录，至少区分：

- 可用现金。
- 已提交申购、尚未扣款或确认的现金。
- 赎回已确认、尚未到账的现金。
- 用户计划投入但尚未实际投入的金额。

### 3.2 组合快照

系统应保存带时间的组合快照，不能只保存一个会不断被覆盖的当前值。快照至少包含：

- 快照时间。
- 每只基金份额、市值和权重。
- 可用现金和待结算现金。
- 组合总权益。
- 使用的净值日期。
- 数据是否完整。

后续增加基金时，新基金只从实际确认的交易时间开始进入真实组合，不得回填到交易前的历史持仓中。

## 4. 新增基金的正确流程

用户新增基金时，应通过交易登记或持仓导入流程完成，而不是把建议直接写成持仓：

```text
用户实际提交买入
    -> 登记交易 submitted
    -> 平台确认净值和份额 confirmed
    -> 更新该基金已确认份额
    -> 生成持仓变更日志
    -> 下一次分析读取新的真实持仓
```

如果用户没有操作：

```text
建议保持 pending、skipped 或 expired
    -> 不创建真实交易
    -> 不新增基金持仓
    -> 原组合维持不变
```

用户自行买入系统从未建议过的基金时，也必须允许手工登记；该交易标记为“用户自主操作”，不能伪装成策略执行结果。

## 5. 追加资金和收益计算

后续追加资金会影响账户金额，但追加资金本身不是盈利。系统必须分开计算：

- **时间加权收益率 TWR**：尽量排除外部入金和出金影响，用于评价策略本身。
- **资金加权收益率/XIRR**：反映用户实际投入时间和金额后的个人收益体验。
- **绝对盈亏**：当前总权益减去累计净投入。

必须单独记录外部现金流：

```text
external_deposit：用户追加资金
external_withdrawal：用户取出资金
trade_cashflow：买卖基金产生的内部现金流
fee：交易或平台费用
```

买卖基金属于组合内部资产转换，不应被当作外部入金或出金。

## 6. 多基金仓位和资金守恒

任意基金数量下均必须满足：

```text
0 <= 单只基金目标权重 <= 单基金上限
所有基金目标权重之和 <= 1
目标现金权重 = 1 - 所有基金目标权重之和
```

当前已确认的组合最大可接受回撤为 20%，但每只基金和每个类别还应有独立的集中度限制。

多基金调仓需要考虑：

- 单基金上限。
- 同行业、同地区和同资产类别的合并暴露。
- 基金之间的相关性。
- 最小申购金额。
- 申购费和持有期赎回费。
- 赎回资金到账延迟。
- 暂停申购、限制大额申购或暂停赎回。

如果卖出资金尚未到账，系统可以给出分阶段计划，但不能把第二阶段买入显示为当前立即可执行。

## 7. 多基金净值日期对齐

不同基金可能在不同日期发布净值，QDII、节假日和暂停估值会导致交易日历不一致。

禁止按数组长度直接截断后计算相关性。必须按照真实日期连接，并明确选择：

- 共同有效日期。
- 在允许范围内使用上一条已知净值。
- 数据缺失过多时拒绝计算。

任何填充规则都要防止使用未来净值。

相关性、协方差和组合回测必须返回实际共同样本数和共同起止日期。

## 8. 多基金基准

每只基金需要独立匹配投资风格基准。组合层基准应根据当时实际持仓或明确的战略权重形成，不得因为第一只基金使用沪深300，就让后来所有基金继续沿用沪深300。

组合基准需要考虑权重随真实交易变化：

```text
组合基准收益 = 各基金匹配基准收益按对应期间组合权重加权
```

如果部分基金基准尚未确认，报告必须显示基准覆盖率，不能静默用同一个指数补齐。

## 9. 本地与 staging 测试矩阵

不需要等待用户真的增加基金才测试多基金能力。至少建立以下合成场景：

| 场景 | 基金数量 | 重点验证 |
| --- | ---: | --- |
| 空组合 | 0 | 页面、报告和建议不崩溃 |
| 当前小额组合 | 1 | 1.48 元持仓、10 元现金、精度和费用 |
| 单基金现金保护 | 1 | 25% 基金、75% 现金不被归一化 |
| 两基金组合 | 2 | 权重、现金、日期对齐和轮换 |
| 五基金混合组合 | 5 | 集中度、分类暴露和组合基准 |
| 十基金组合 | 10 | 性能、报告可读性和优化稳定性 |
| 中途新增基金 | 1 变 2 | 新基金不污染此前历史持仓 |
| 中途追加资金 | 任意 | TWR、XIRR 和绝对盈亏分离 |
| 部分执行 | 任意 | 只按实际成交更新持仓 |
| 不采纳建议 | 任意 | 持仓保持原样 |
| 异步净值日历 | 3 以上 | QDII、节假日和缺失日期 |
| 赎回未到账 | 2 以上 | 不提前复用现金 |

测试金额同时覆盖：

- 1 元级小额。
- 100 元级。
- 10000 元级。
- 含多位小数的份额和净值。

金额增大不应改变策略方向；只允许因为费用、最小交易额、舍入和风险约束而产生可解释差异。

## 10. 当前单一持仓对开发的实际影响

### 不受影响的部分

- 最新净值窗口修复。
- 现金仓位修复。
- 零目标修复。
- 建议与成交状态分离。
- 费用、成交延迟和总收益净值。
- 动作验证和命中率口径。
- staging 环境建设。

### 不能仅靠当前真实数据证明的部分

- 分散化是否有效。
- 相关性和协方差是否稳定。
- 多基金再平衡是否正确。
- 组合基准是否合理。
- 不同基金类型的参数是否合适。
- 最大夏普、风险平价等组合算法是否优于简单基线。

这些部分必须通过合成数据、历史公开数据和滚动样本外验证完成，不能因为当前只有一只基金就跳过测试，也不能假装当前真实数据已经证明多基金能力。

## 11. 验收条件

- 新增第二只基金不需要修改表结构或核心算法接口。
- 新增基金只从实际确认时间进入真实组合。
- 未操作的建议不改变基金数量、份额或现金。
- 追加资金不会被统计为投资收益。
- 组合持仓、现金和待结算资产始终守恒。
- 任意基金数量下目标权重总和不超过 1。
- 多基金净值和基准均按日期对齐。
- 单基金和多基金使用同一套核心接口，不存在只为当前基金编写的特殊生产分支。
- 所有组合结果显示基金数量、数据截止日期、基准覆盖率和费用假设。

满足上述要求后，用户未来增加基金只属于正常数据和交易变化，不会影响后续开发架构。
