# My Funds 盈利导向修复与开发计划

> 文档状态：待执行
>
> 编写日期：2026-08-28
>
> 适用项目：`my-funds`
>
> 执行对象：后续接手开发的 Luna/Codex 模型
>
> 核心原则：先保证数据、仓位、成交和验证结果可信，再讨论参数优化和扩大资金。

---

## 1. 项目目标与当前结论

本项目的最终目标是帮助用户管理真实基金持仓，在风险可接受的前提下提高扣费后收益，而不是单纯生成日报、提高表面命中率或让回测曲线更好看。

当前服务器中的真实数据是业务事实来源，本地只用于代码开发和合成数据测试：

- 当前真实持仓总市值约为 `1.48 元`。
- 当前设置的可用资金约为 `10 元`。
- 当前阶段属于小额真实验证盘，适合验证数据、建议和执行链路，不适合用绝对盈利金额判断策略好坏。
- 服务器已经积累约 30 份报告；建议记录数量多于报告数量，不能把每条建议视为相互独立的样本。
- 当前展示的约 85% 命中率只来源于少量被识别为方向性建议的记录，不能代表全部建议的真实盈利能力。

代码审计结论：现有系统已经具备净值采集、持仓分析、报告、建议、回测和参数优化的基本框架，但存在会直接扭曲仓位、收益和命中率的 P0 级问题。在这些问题修复前，不允许依据现有回测结果提高仓位，也不建议继续优化策略参数。

### 1.1 不能混淆的三个目标

1. **建议正确性**：系统是否基于当时可获得的数据给出了合理动作。
2. **策略盈利性**：把建议按照现实规则执行后，扣除费用是否获得了更好的风险调整后收益。
3. **系统稳定性**：采集、报告和服务是否持续正常运行。

服务稳定不代表策略盈利；方向命中也不等于扣费后盈利；回测优于基准也不代表未来一定盈利。

### 1.2 本轮开发的成功定义

第一轮开发成功不以“收益率提高”为标准，而以以下事实成立为标准：

- 回测严格使用信号产生后才能知道的净值。
- 策略输出 25% 仓位时，模拟结果保持 25% 持仓和 75% 现金。
- 策略输出 0% 仓位时，不会被改成满仓或等权仓位。
- 收益计算能够正确处理基金分红，不把分红误判为亏损。
- 申购费、赎回费、确认延迟和资金到账延迟均可配置并进入结果。
- 买入、加仓、减仓、卖出等动作都使用一致的验证口径。
- 不同基金使用与投资风格匹配的基准。
- 所有新结果均标注模型版本、数据截止时间、费用假设和执行假设。

---

## 2. 开发与服务器边界

### 2.1 数据和代码的事实来源

- **生产数据事实来源**：服务器数据库。
- **当前生产代码事实来源**：服务器正在运行的代码，直到完成一次服务器与本地代码差异核对。
- **本地开发目录**：`E:\myfund11111`。
- **本地不得长期保存**：真实数据库完整副本、数据库密码、服务器私钥、用户隐私数据和生产环境 `.env`。
- **本地允许保存**：脱敏后的最小测试夹具、合成净值序列、数据库结构说明和不含秘密的配置模板。

### 2.2 接手模型开始开发前必须执行的只读检查

下面命令均为示例。执行前需要先确认项目实际 Python 环境、服务管理方式和目录结构；不得把示例中的占位符原样用于生产。

```powershell
# 查看本地工作区是否存在未提交修改，避免覆盖用户工作
git -C E:\myfund11111 status --short

# 查看本地分支及最近提交，建立本地代码基线
git -C E:\myfund11111 branch --show-current
git -C E:\myfund11111 log -5 --oneline
```

服务器检查应通过本机 SSH 配置别名完成，仓库内不得写入服务器 IP、用户名或私钥路径：

```powershell
# 只读查看服务器部署目录中的 Git 状态；DEPLOY_DIR 需替换为已核实的部署目录
ssh my-funds-prod "git -C DEPLOY_DIR status --short"

# 只读查看服务器正在运行的提交
ssh my-funds-prod "git -C DEPLOY_DIR rev-parse HEAD"
```

必须先生成差异清单，再决定同步方向。用户已经明确“先以服务器为准”，因此不得直接用本地代码覆盖服务器。

### 2.3 开发纪律

- 第一阶段只修正确性，不顺便调整策略参数。
- 每个缺陷先写失败测试，再修改实现。
- 每次提交只解决一个可以独立验证的问题。
- 业务代码中的新增注释、文档和用户提示使用中文。
- 不把测试请求发送到生产数据库。
- 不在生产环境执行自动调参。
- 不删除历史报告和历史建议；需要清理时先备份、迁移并获得确认。
- 不把服务器凭据、SSH 私钥、数据库密码提交到 Git。

---

## 3. 已确认的 P0 问题

## 3.1 历史净值窗口取到了最旧记录

### 位置

`fund-advisor/backend/services/advisor_service.py` 的 `_load_nav_history` 附近。

### 当前行为

查询按照 `nav_date.asc()` 排序后直接 `limit(252)`。当某只基金历史记录超过 252 条时，取得的是最早的 252 条，而不是最近的 252 条。

这会导致同一份报告中出现口径分裂：当前市值可能使用最新净值，但趋势、动量、回撤和夏普率可能使用旧年份数据。

### 修复要求

推荐实现：

1. 数据库查询按日期倒序取最近 N 条。
2. 取出后再按日期升序排列，供时间序列算法使用。
3. 返回结果必须显式包含 `start_date`、`end_date` 和实际样本数。
4. 报告生成时校验历史序列最后日期等于该基金最新可用净值日期。
5. 数据不足时输出“样本不足”，不得静默使用陈旧窗口。

### 必须新增的测试

- 创建 300 条连续净值，断言返回第 49 至第 300 条，共 252 条。
- 打乱插入顺序，断言结果仍按日期升序。
- 包含重复日期时，断言按照数据库约束或去重规则只保留一条。
- 不足 252 条时，断言返回全部记录并正确报告样本数。

### 验收条件

- 任一历史超过 252 条的基金，窗口结束日期必须等于数据库中的最新净值日期。
- 报告展示的数据截止日期与实际计算窗口一致。

---

## 3.2 模拟器破坏现金仓位

### 位置

`fund-analyzer/engine/simulator.py` 中目标权重归一化逻辑附近。

### 当前行为

策略生成目标权重后，模拟器把所有非零目标权重重新归一化为总和 1。

单基金情况下：

- 请求 25% 仓位，执行后变成 100%。
- 请求 50% 仓位，执行后变成 100%。
- 请求 80% 仓位，执行后仍为 100%。

这会使目标波动率、熊市仓位上限、集中度上限、部分减仓和现金保护在回测中失效。

### 修复要求

目标权重必须代表整个资产组合中的真实比例，包括现金：

```text
0 <= 每项基金目标权重 <= 1
基金目标权重之和 <= 1
现金目标权重 = 1 - 基金目标权重之和
```

- 权重之和小于 1 时必须保留现金，不得归一化。
- 权重之和大于 1 时，开发和测试环境应直接报错并记录策略输出；不能静默修正掩盖上游错误。
- 若生产环境为了容错需要缩放，必须同时记录告警、原始权重和修正后权重，不得无痕处理。
- 浮点误差只允许在很小容差范围内处理，例如 `1e-9`。

### 必须新增的测试

- 单基金目标 0.25，最终基金权重 0.25、现金权重 0.75。
- 单基金目标 0，最终基金权重 0、现金权重 1。
- 两只基金目标 0.3 和 0.2，最终现金权重 0.5。
- 权重总和 1.01 时抛出明确异常。
- 由于浮点误差形成 `1.0000000001` 时按容差处理。

### 验收条件

现有最小复现用例中，目标 25% 不再变成满仓。

---

## 3.3 清仓目标被配置器反转成满仓

### 位置

`fund-analyzer/engine/allocation.py` 中目标权重总和小于等于 0 时的兜底逻辑附近。

### 当前行为

当所有目标权重为 0 时，配置器会把目标改为等权。单基金情况下等价于从清仓变成 100% 持仓。

在当前约 1.48 元持仓和 10 元可用资金的场景中，最小复现会生成目标金额约 11.48 元并建议加仓约 10 元。

### 修复要求

- 全部目标权重为 0 必须解释为全部现金。
- 不允许用等权作为零目标的兜底。
- 若策略没有返回目标，应使用独立的“缺少决策”状态，不能和明确的零仓位混淆。
- 资金配置统一基于：

```text
组合总权益 = 当前持仓市值 + 可用现金
目标基金金额 = 组合总权益 × 目标权重
目标现金金额 = 组合总权益 × 现金权重
```

- 轮换操作需要区分“理论上卖出后可用的资金”和“当前已经到账可用的资金”。开放式基金赎回资金未到账前，不得给出可立即同时执行的买入指令。
- 可用资金为 0 时，不得生成无资金来源的新增买入。

### 必须新增的测试

- 当前持仓 1.48、现金 10、目标 0，结果为减仓 1.48、目标现金 11.48，不产生加仓。
- 当前持仓 1.48、现金 0、目标 1，不产生额外买入。
- 两只基金轮换时，卖出资金未到账前，新买入状态为“等待资金确认”。
- 目标基金金额总和加目标现金金额等于组合总权益，误差在货币精度范围内。

---

## 3.4 回测存在同日未知净值成交偏差

### 位置

`fund-analyzer/engine/simulator.py` 的信号生成和默认执行器附近。

### 当前行为

策略使用截至日期 D 的净值生成信号，又按照日期 D 的净值完成调仓。开放式基金在申购或赎回时通常并不知道当日最终净值，因此这一实现会形成前视偏差。

### 修复要求

建立独立的信号时间和执行时间：

```text
signal_date：生成建议时已经可获得的数据截止日期
order_date：提交订单日期
execution_nav_date：订单实际使用的净值日期
settlement_date：份额或资金可用日期
```

第一版保守规则：

- 信号只使用 `signal_date` 当时已经发布的净值。
- 默认按照下一条可用净值执行，禁止同条净值同时参与信号和成交。
- 申购后在确认日之前不得参与再次赎回。
- 赎回资金在到账日之前不得用于新的申购。
- 每笔模拟交易记录信号日期、执行净值日期、费用和到账日期。

后续可以按基金和平台配置更精确的截单时间，但首版不能继续假设无延迟同日成交。

### 必须新增的测试

- D 日产生信号，断言使用 D 之后第一条净值成交。
- 周末产生信号，断言使用下一个存在净值的日期。
- 净值中间缺失，断言不会用未来数据回填信号输入。
- 赎回到账前，现金余额不可再次使用。

---

## 3.5 回测没有交易费用和持有期限制

### 当前行为

申购、赎回和轮换基本按无费用、即时到账处理。这样会高估频繁调仓策略，尤其是小金额账户。

### 修复要求

新增统一的费用与执行规则对象，至少支持：

- 申购费率及优惠费率。
- 按持有天数分段的赎回费率。
- 固定最低费用，如果实际平台存在。
- 申购确认天数。
- 赎回到账天数。
- QDII、跨境和特殊基金的额外延迟。
- 金额和份额的小数精度规则。

所有回测必须同时输出：

- 毛收益。
- 申购费用。
- 赎回费用。
- 总费用。
- 扣费后收益。
- 换手率。
- 因等待确认而损失的可交易天数。

如果真实费率暂时未知，必须使用明确标注的保守默认值，并在报告上显示“估算费用”，不得按 0 处理。

---

## 3.6 收益序列未正确处理分红

### 位置

- `fund-advisor/backend/services/advisor_service.py`
- `fund-advisor/backend/services/simulator_service.py`
- `fund-advisor/backend/services/backtest_service.py`

### 当前行为

分析和验证主要使用 `unit_nav`。发生现金分红时，单位净值会机械下降；如果不做复权，总收益和回撤都可能被错误计算。

数据库已有 `acc_nav` 信息，但不能在没有检查数据含义和完整性的情况下直接全局替换。

### 修复要求

统一建立“展示净值”和“收益净值”两个概念：

- `display_nav`：用于页面展示，通常为单位净值。
- `return_nav`：用于收益、波动率、回撤和回测，应优先使用可靠的复权净值或总回报指数。

实施步骤：

1. 检查当前数据源中 `acc_nav` 的定义、缺失率和历史连续性。
2. 对发生过分红的基金建立人工核对样例。
3. 如果 `acc_nav` 只能表示累计分红而不是分红再投资总回报，需要生成明确的复权净值序列。
4. 禁止在同一条收益序列中无提示地混用单位净值和累计净值。
5. 数据源变更要增加 `nav_method_version`，防止新旧报告混算。

### 必须新增的测试

- 构造单位净值因分红从 1.20 降至 1.10、累计/复权净值保持总价值连续的样例。
- 断言系统不会把分红日识别为约 8.33% 的真实亏损。
- 断言显示页面仍可以展示单位净值。

---

## 3.7 建议动作映射和命中率分母错误

### 位置

`fund-analyzer/engine/backtest.py` 以及报告统计服务。

### 当前行为

方向性动作只识别部分名称。`buy`、`sell`、`decrease` 等动作会被当成中性，从而不进入方向命中率分母。当前服务器统计约 109 条建议、92 条已验证，但只有约 20 条进入方向命中率，17 次命中形成约 85% 的表面数值。

不同统计位置对中性动作是否进入分母的处理也不一致。

### 修复要求

建立唯一动作枚举和统一映射：

```text
正向：buy、increase
负向：sell、reduce、decrease
中性：hold、watch
```

数据库历史动作需要提供兼容映射，不要直接删除旧值。

报告至少拆分展示：

- 总建议数。
- 已到验证期数量。
- 尚未到验证期数量。
- 方向性建议数量。
- 中性建议数量。
- 方向命中率 = 方向命中数 / 可验证方向性建议数。
- 方向覆盖率 = 方向性建议数 / 已验证建议数。
- 扣费后平均收益。
- 相对基准的平均超额收益。

页面不得只展示一个没有分母和覆盖率的“命中率”。

### 必须新增的测试

- `buy` 和 `increase` 都按照上涨方向验证。
- `sell`、`reduce`、`decrease` 都按照下跌或跑赢持有方案的方向验证。
- `hold`、`watch` 不进入方向命中率，但进入覆盖率统计。
- 同一组数据在总览和按动作统计中的分母一致。

---

## 3.8 所有基金使用固定沪深300基准

### 位置

- `fund-advisor/backend/services/index_bindings.py`
- `fund-advisor/backend/services/backtest_service.py`

### 当前行为

大量基金默认使用沪深300，只有少量基金有硬编码映射。债券、海外、行业、商品和偏小盘基金使用沪深300比较时，会得到失真的超额收益和动作命中结果。

### 修复要求

新增可配置的基准映射，优先级建议如下：

1. 基金正式披露的业绩比较基准。
2. 与基金投资范围和风格匹配的指数。
3. 同类别基金指数或可解释的组合基准。
4. 无法确认时标记“基准待确认”，不得静默使用沪深300。

每次验证记录以下字段：

- `benchmark_code`
- `benchmark_name`
- `benchmark_version`
- `benchmark_source`
- 基金和基准实际对齐的起止日期

基金和基准收益必须按实际日期连接，不能按数组长度截断，也不能因为某日缺失就退回绝对收益口径。

---

## 4. P1 策略与统计优化

P1 任务只能在全部 P0 验收通过后开始。

## 4.1 信号存在重复计分和概念混用

当前方向与择时逻辑同时使用动量、均线、RSI、回撤和净值分位。这些指标大量来自同一条价格序列，相关性较高，不能把多个相似信号简单相加后当作多重独立证据。

重点问题：

- 净值位于历史低位不等于底层资产估值便宜。
- 将夏普率命名为“价值分”概念不正确。
- RSI 超卖属于均值回归倾向，动量和均线属于趋势倾向，两者冲突时缺少状态规则。
- 接近历史最大回撤时给予高抄底分数，可能在持续下跌中不断加仓。
- 固定 25% 回撤止损可能在深度下跌后集中卖出，又缺少明确再入场机制。

### 建议改造

将信号拆成互不混淆的层次：

1. **长期资产配置层**：基金类型、风险预算、相关性和集中度。
2. **中期趋势层**：3、6、12 月动量和长期均线状态。
3. **短期执行层**：避免追涨、控制一次调仓幅度和费用。
4. **风险覆盖层**：波动率目标、最大回撤、数据异常和流动性。

第一版候选策略应保持简单：

- 中期动量使用多个期限组合，防止单一期限决定全部仓位。
- 长期趋势过滤只负责允许或限制风险仓位。
- RSI 和净值分位暂时退出核心仓位计算，只作为解释信息或对照实验。
- 所有信号权重通过滚动样本外结果确定，不手工根据回测最好曲线反复调整。

## 4.2 固定风险参数不适用于所有基金

当前类似 15% 目标波动率、25% 最大回撤、50% 单基金上限和熊市 30% 仓位上限等参数，需要按基金类型和用户风险承受能力调整。

建议建立：

- 用户级最大可接受回撤。
- 基金类型级目标波动率范围。
- 单基金和同主题基金合并集中度上限。
- 现金最低比例。
- 单次最大调仓比例。
- 信号进入和退出的滞回区间，避免反复横跳。

在用户尚未确认风险上限前，不得把“最大盈利”解释成可以无限增加波动和回撤。

## 4.3 最大夏普组合需要降级为对照项

### 位置

`fund-analyzer/engine/portfolio_quant.py` 及调用组合建议的位置。

### 当前风险

- 很短的历史窗口用于估计年化收益。
- 数据按长度截断而不是按日期对齐。
- 持仓较多时只取前几只，缺少明确选择依据。
- 随机搜索最大夏普权重对预期收益估计误差极其敏感。
- 优化结果直接用于加减仓建议，缺少稳定性和费用检查。

### 改造方向

- 所有资产收益按日期内连接或明确的缺失规则对齐。
- 至少提供等权、当前持有、风险平价和最小方差作为基线。
- 使用协方差收缩或其他稳健估计。
- 限制单资产、单类别、单主题和单次调仓幅度。
- 只有在多个样本外窗口持续优于简单基线且扣费后改善足够大时，优化权重才允许进入建议。

---

## 5. 回测系统重构规格

## 5.1 明确回测对象

回测结果必须区分：

- 单个信号因子的研究回测。
- 完整策略回测。
- 当前真实持仓的历史模拟。
- 参数候选的滚动样本外验证。
- 生产建议的事后真实验证。

不得把这些结果合并成一个“盈利概率”。

## 5.2 真实初始持仓

当前模拟器默认等权初始化，多基金情况下无法代表真实组合。

接口应接收：

- 每只基金的真实份额或市值。
- 可用现金。
- 成本信息。
- 已提交但尚未确认的订单。
- 尚未到账的赎回资金。

买入持有基线也必须使用相同初始持仓，而不是重新等权。

## 5.3 时间轴和无前视原则

每个回测日应按固定顺序处理：

1. 处理当天已经确认的份额和到账资金。
2. 向策略提供截至当时已经发布的数据。
3. 策略生成目标仓位。
4. 订单系统根据限制生成订单。
5. 在未来可用净值上成交。
6. 扣除费用并更新待确认状态。
7. 记录日终持仓、现金和总权益。

禁止先读取当天最终净值，再假设可以用该净值无延迟成交。

## 5.4 基准和评价指标

默认必须报告：

- 时间加权收益率。
- 年化收益率，仅在时间跨度足够时展示。
- 最大回撤及恢复时间。
- 年化波动率。
- Sortino 比率。
- 扣费前后收益差。
- 换手率和总费用。
- 相对匹配基准的超额收益和信息比率。
- 相对真实持有不动方案的收益改善。
- 现金平均占比和最大占比。

小于一年时不要突出年化收益；样本很小时不得显示高精度小数造成虚假确定感。

## 5.5 禁止继续使用的误导性标签

- 不得把“单条路径中净值高于起点的天数占比”命名为“盈利概率”。
- 不得把三个重叠窗口中两个优于基准称为“高置信度盈利”。
- 不得只展示命中率而隐藏样本数、覆盖率和中性建议。
- 不得把训练期最优结果称为预期收益。

可以替换为：

- 样本外窗口通过率。
- 扣费后超额收益为正的窗口比例。
- 当前证据等级：不足、初步、一般、较强。
- 明确显示独立窗口数量和总跨度。

---

## 6. Walk-Forward 与自学习重构

### 6.1 当前问题

`fund-analyzer/engine/adaptive_optimizer.py` 当前主要使用一次 60/40 切分，不是真正的滚动前推验证。参数组合从较短训练期中选择后，只要测试结果略好就可能通过；费用、参数试验次数和统计不确定性没有充分处理。

所谓 `rolling_window=10` 的自学习流程也没有真正只使用最近 10 个有效样本，而且每日生成的 20 日标签高度重叠，不能按独立样本理解。

### 6.2 目标流程

采用滚动或扩展窗口：

```text
训练窗口1 -> 验证窗口1
训练窗口2 -> 验证窗口2
训练窗口3 -> 验证窗口3
……
```

最低要求：

- 至少 5 个样本外窗口；数据不足时直接标记证据不足。
- 参数只能在训练窗口选择。
- 样本外窗口结果不得反馈到同一窗口参数。
- 标签持有期为 20 日时，应采用适当的 purge/embargo，减少训练和验证标签重叠。
- 所有窗口扣除费用和延迟。
- 保存每个候选参数在所有窗口的完整结果，不只保存冠军。

### 6.3 参数准入规则

参数不得因为一次测试略微获胜就自动进入生产。建议同时满足：

- 多数样本外窗口扣费后优于默认策略。
- 平均改善大于费用和估计误差形成的最低门槛。
- 最大回撤没有突破用户风险上限。
- 换手率没有显著恶化。
- 不依赖单个窗口或单次极端行情贡献全部收益。
- 对邻近参数保持稳定，不是尖锐的单点最优。

可增加 Deflated Sharpe、参数选择偏差或回测过拟合概率作为研究指标，但不要把统计指标包装成盈利保证。

### 6.4 生产参数更新

- 自动优化只能生成候选配置。
- 候选配置先进入影子验证，不直接影响生产建议。
- 生产参数变更需要版本号、变更原因、训练区间、验证区间和回滚配置。
- 生产报告必须记录本次使用的参数版本。

---

## 7. 建议快照、历史统计和数据库要求

### 7.1 当前风险

报告保留数量和建议快照数量的生命周期可能不一致，容易出现旧快照继续参与统计、报告已经删除但建议仍存在的情况。建议快照也需要防止同一报告、同一基金被重复写入。

### 7.2 推荐约束

- 为建议快照建立稳定主键。
- 对 `(report_id, fund_code)` 建立唯一约束；如果同一报告允许多个策略版本，则唯一键中加入 `model_version`。
- `report_id` 应建立真实外键；是否级联删除必须先确定历史保留政策。
- 历史统计默认按当前模型版本分组，不把旧版本和新版本直接混合。
- 每条建议保存输入数据截止日期、执行假设版本、基准版本、费用版本和策略版本。
- 报告清理任务不得静默删除其对应的审计数据。

### 7.3 数据库迁移纪律

1. 先只读统计空值、重复行和孤立记录。
2. 生成迁移 SQL 和回滚 SQL。
3. 在隔离数据库或临时库验证。
4. 备份生产数据库。
5. 先增加可空字段并兼容旧代码，再回填数据。
6. 验证完成后再添加非空或唯一约束。
7. 迁移后核对记录总数、重复数和关键业务查询。

禁止在没有备份和回滚方案的情况下直接修改生产表结构。

---

## 8. 分阶段开发任务

## 阶段 A：建立基线，不改变策略行为

### 任务

- 核对服务器代码、本地代码和 GitHub 提交差异。
- 记录服务器运行版本、Python 版本、依赖锁和服务启动方式。
- 执行现有测试并保存测试数量和结果；此前曾观察到约 148 个测试通过，但接手模型必须重新验证当前基线。
- 为 P0 缺陷添加可复现的失败测试。
- 记录当前服务器仪表盘核心数值，作为部署后数据完整性对照。

### 完成标准

- 有明确的代码基线提交。
- 现有测试结果可重复。
- 每个 P0 问题至少有一个修改前会失败的测试。

## 阶段 B：净值和仓位正确性

### 任务顺序

1. 修复最近 252 条净值窗口。
2. 修复目标权重与现金保留。
3. 修复零目标配置。
4. 增加权重和资金守恒断言。
5. 增加真实初始持仓接口。

### 完成标准

- 所有资金守恒测试通过。
- 单基金 25% 目标仓位保持 75% 现金。
- 零目标不会产生买入。
- 不会生成超过可用资金来源的订单。

## 阶段 C：真实执行与总收益

### 任务顺序

1. 拆分信号、订单、成交和结算日期。
2. 新增费用与确认规则。
3. 建立收益净值统一接口。
4. 修复基金和基准日期对齐。
5. 重新计算历史回测，但不要覆盖旧结果；使用新版本保存。

### 完成标准

- 无同日未知净值成交。
- 费用和延迟改变结果时能够被测试观测。
- 分红样例不会产生虚假亏损。

## 阶段 D：验证指标和页面文案

### 任务顺序

1. 统一动作枚举。
2. 修复方向命中率、覆盖率和分母。
3. 增加风格基准映射。
4. 移除“盈利概率”“高置信度盈利”等不严谨名称。
5. 页面显示样本量、数据截止日期、费用和执行假设。

### 完成标准

- 所有动作分类一致。
- 用户可以看到 85% 之类指标的分子、分母和覆盖率。
- 没有基准的基金不会静默使用沪深300。

## 阶段 E：滚动验证和策略优化

### 任务顺序

1. 实现真实滚动 Walk-Forward。
2. 增加简单基线策略。
3. 分离动量、趋势、执行和风险层。
4. 将 RSI、净值分位等作为可关闭的实验因子。
5. 建立影子参数和生产参数双轨制。

### 完成标准

- 至少 5 个独立样本外窗口。
- 新参数扣费后稳定优于简单基线，且不突破风险限制。
- 参数变更可以一键回滚到上个版本。

---

## 9. 测试清单

接手模型应根据仓库实际测试结构放置测试，不要为了匹配本清单而破坏现有目录约定。

### 9.1 单元测试

- 最新净值窗口和日期顺序。
- 单位净值、累计净值、复权收益序列。
- 目标权重合法性。
- 现金权重守恒。
- 零仓位。
- 可用现金为零。
- 卖出资金未到账。
- 各动作的方向映射。
- 费用分段。
- 下一可用净值成交。
- 周末和净值缺失。
- 基准日期对齐。
- 多基金真实初始权重。
- 参数版本与结果版本隔离。

### 9.2 集成测试

- 从持仓读取到报告生成的完整链路。
- 从建议快照到到期验证的完整链路。
- 从策略目标到订单、成交、费用和结算的完整链路。
- 使用临时数据库验证唯一约束和迁移。
- 重复执行报告任务不会产生重复建议快照。

### 9.3 回归测试

- 当前真实持仓金额不会因精度或单位错误放大 100、10000 倍。
- 当前可用资金 10 元不会被解释为 10 万或 10%。
- 报告、API 和前端仍以元为金额单位。
- 修复后旧报告仍可打开；旧版本指标有明确标记。
- 服务重启后定时任务不会重复补跑同一报告。

### 9.4 性质测试建议

对于仓位和资金逻辑，可以增加性质测试：

```text
任意合法目标权重下：
基金市值总和 + 现金 = 组合总权益 - 已扣费用

没有外部入金时：
订单不能创造现金或份额

目标权重全部为零时：
目标基金金额全部为零
```

---

## 10. 部署、验证与回滚

## 10.1 部署前

- 确认服务器代码无未保存修改。
- 备份数据库，并验证备份文件可以读取。
- 保存当前运行提交、依赖版本和服务配置摘要。
- 记录部署前的持仓总额、基金数、报告数、建议数、最新净值日期。
- 在本地和隔离环境运行完整测试。
- 禁止将本地合成数据写入生产库。

## 10.2 推荐发布方式

- 每个阶段独立提交和打标签。
- 先部署只影响计算但不覆盖旧结果的新版本。
- 新旧回测引擎并行运行一次，比较差异原因。
- 第一轮发布保持建议只读，不执行自动交易。
- 定时任务首次运行时人工观察日志和数据库写入数量。

## 10.3 部署后核对

- 服务健康检查正常。
- 最新净值日期没有倒退。
- 真实持仓仍约为部署前数值，允许正常净值变化，不允许数量级突变。
- 报告只新增预期数量。
- 建议快照不重复。
- 单基金 25% 测试策略仍保留 75% 现金。
- 新版命中率页面显示分子、分母和覆盖率。
- 日志中没有无声的权重归一化、基准回退或费用缺失。

## 10.4 回滚触发条件

出现以下任一情况立即停止定时任务并回滚：

- 持仓或金额发生数量级变化。
- 生成无资金来源的买入建议。
- 明确清仓信号被反转为买入。
- 最新净值日期倒退到旧年份。
- 报告或建议快照异常重复。
- 数据库迁移后关键记录数不一致。
- 新版服务持续报错或无法生成报告。

代码回滚和数据库回滚必须分开设计；不能只回滚代码却留下新代码才识别的不可逆表结构。

---

## 11. 代码提交建议

建议拆分为以下提交，不要做成一个大型提交：

1. `test: 增加盈利链路关键缺陷复现测试`
2. `fix: 使用最新净值窗口并校验数据截止日期`
3. `fix: 保留现金权重并修复零仓位配置`
4. `feat: 增加订单成交和结算时间轴`
5. `feat: 增加基金费用与扣费后收益`
6. `fix: 使用统一总收益序列处理分红`
7. `fix: 统一建议动作和验证统计口径`
8. `feat: 增加基金风格基准映射`
9. `refactor: 重构滚动前推验证和参数版本`
10. `docs: 更新指标定义、部署和回滚说明`

每个提交说明中应包含：

- 原问题。
- 为什么会影响盈利判断。
- 修改方式。
- 新增测试。
- 对数据库和旧报告的兼容影响。

---

## 12. 接手 Luna 的执行提示

1. 首先完整阅读本文档和仓库中的 `AGENTS.md`、README、部署文档。
2. 不要根据本文档直接猜测服务器目录、服务名或数据库类型，先只读核实。
3. 用户已经指定服务器代码和数据优先，先做服务器与本地差异审计。
4. 不要在第一阶段重新设计前端或更换技术栈。
5. 不要因为当前只有约 1.48 元持仓就简化金额精度；小额场景正好可以暴露费用和舍入问题。
6. 不要为了让回测变好而修改测试期、基准或样本窗口。
7. 所有策略改动必须与简单持有、等权和匹配基准比较。
8. 所有修改先在本地使用合成数据和临时数据库测试。
9. 涉及生产数据库结构、删除历史数据、覆盖服务器代码或重启生产服务前，先向用户说明影响并请求确认。
10. 每完成一个阶段，向用户汇报：修改文件、测试结果、指标变化、遗留风险和是否建议部署。

---

## 13. 需要用户确认的业务问题

以下问题不会阻止 P0 正确性修复，但会影响后续策略和风险参数：

1. **最大可接受回撤**：用户能接受组合从阶段高点下跌多少？建议明确 10%、20% 或其他数值。
2. **操作模式**：长期保持“系统给建议、用户手动交易”，还是未来考虑自动下单？第一阶段默认只给建议，不自动交易。
3. **真实费用来源**：当前持仓所在平台、申购费折扣、赎回费率和持有时间是否已经记录在数据库中？如果没有，需要补充。
4. **现金流计划**：未来是否会定期追加资金？定投和一次性资金需要不同的评价基线。
5. **风险目标优先级**：用户更重视最大化长期收益，还是更重视控制回撤和保持资金稳定？不能只用“盈利”推导唯一参数。
6. **隔离验证环境**：是否允许在同一服务器建立一个不对公网开放的 staging 服务和独立测试数据库？生产数据库不能承担开发测试。

在用户回答前采用以下保守默认：

- 不自动交易。
- 不自动启用优化后的参数。
- 不使用生产数据库做测试。
- 不把卖出未到账资金视为可用现金。
- 未知费用使用显式标注的保守估计。
- 未确认基准时不计算或不宣传超额收益。

---

## 14. 第一批实际开发范围

如果用户批准开始修复，第一批仅包含以下内容：

1. 增加 P0 缺陷复现测试。
2. 修复最近净值窗口。
3. 修复模拟器现金权重。
4. 修复零目标配置和资金守恒。
5. 统一建议动作枚举与统计分母。
6. 运行完整测试并生成差异报告。

第一批明确不包含：

- 调整动量、RSI、回撤或目标波动率参数。
- 重写前端。
- 自动交易。
- 删除历史报告。
- 直接覆盖生产代码。
- 使用当前回测结果增加资金。

第一批通过后，再进入成交延迟、费用、总收益净值和基准体系重构。

---

## 15. 最终验收总表

| 验收项 | 必须结果 |
| --- | --- |
| 最新净值 | 计算窗口结束日期等于最新可用净值日期 |
| 25% 目标仓位 | 基金 25%，现金 75% |
| 0% 目标仓位 | 不产生买入，目标为全部现金 |
| 资金守恒 | 持仓 + 现金 + 待结算资产扣除费用后守恒 |
| 成交时间 | 不使用同一未知净值同时生成信号和成交 |
| 交易费用 | 报告毛收益、费用和净收益 |
| 分红处理 | 不把分红导致的单位净值下降视为真实亏损 |
| 动作验证 | buy/increase/sell/reduce/decrease 全部映射正确 |
| 命中率 | 展示分子、分母、覆盖率和样本数 |
| 业绩基准 | 与基金风格匹配，未知时明确标记 |
| WFA | 多个滚动样本外窗口，训练与测试严格隔离 |
| 参数上线 | 先影子验证，可追踪版本，可回滚 |
| 生产安全 | 测试不连接生产数据库，不提交任何秘密 |

只有以上关键项完成后，系统才具备继续研究“如何提高盈利能力”的可靠基础。
