# My Funds 已确认业务规则

> 确认日期：2026-08-28
>
> 本文是 `DEVELOPMENT_PLAN_PROFITABILITY.md` 的业务决策补充。后续 Luna/Codex 开始开发前必须同时阅读两份文档；如果两份文档存在冲突，以本文中已经由用户明确确认的规则为准。

## 1. 已确认事项

1. 组合从阶段高点的最大可接受回撤暂定为 `20%`。
2. 允许在服务器建立独立 staging 服务和独立测试数据库。
3. staging 默认不对公网开放，不得与生产数据库共用写入账号。
4. 系统只提供建议，不连接自动下单。
5. 用户自行在基金平台完成操作。
6. 只有用户确认实际操作后，系统才能同步更新持仓。
7. 如果用户没有操作，真实持仓必须维持原样。

## 2. 最大回撤规则

- `20%` 是策略风险上限和告警阈值，不是保证实际回撤绝不会超过 20%。
- 回测、滚动验证和生产建议均需报告最大回撤。
- 候选策略在样本外回撤超过 20% 时，不得自动进入生产参数。
- 当生产组合接近风险上限时，系统应优先降低风险，并向用户解释触发依据。
- 不得为了提高回测收益而静默放宽 20% 上限。

## 3. 建议与持仓必须分离

生成建议不代表用户已经成交。建议状态与真实交易状态必须分开保存。

建议状态建议采用：

```text
generated：建议刚生成
pending：等待用户决定
skipped：用户明确不操作
expired：建议超过有效期
executed：用户确认全部执行
partially_executed：用户确认部分执行
```

只有 `executed` 和 `partially_executed` 可以进入持仓同步流程。

以下状态不得修改真实持仓：

- `generated`
- `pending`
- `skipped`
- `expired`

后台定时任务不得根据建议内容自动假设用户已经买入或卖出。

## 4. 用户操作登记流程

前端应为可执行建议提供：

- 确认已执行。
- 确认部分执行。
- 暂不操作。
- 忽略本次建议。

用户确认操作时至少登记：

- 基金代码。
- 买入、卖出、加仓或减仓方向。
- 实际提交时间。
- 实际金额或份额。
- 实际成交净值；尚未确认时标记为待确认。
- 实际费用；暂时未知时标记待补充，不能永久按零费用处理。
- 可选的平台订单号或备注。

建议状态、订单提交状态、基金平台确认状态和资金到账状态必须区分：

```text
用户确认操作
    -> submitted：已经提交，尚未确认净值或份额
    -> confirmed：平台已经确认净值和份额
    -> settled：赎回资金已经到账或申购份额已经可用
```

## 5. 持仓同步规则

### 买入

```text
平台确认后：
新份额 = 原份额 + 实际确认份额
可用现金 = 原可用现金 - 实际扣款金额 - 相关费用
```

在平台尚未确认份额时，交易记录可以进入待确认状态，但不得提前增加已确认持仓份额。

### 卖出

```text
平台确认后：
新份额 = 原份额 - 实际确认卖出份额
赎回金额进入待到账状态

资金到账后：
可用现金 = 原可用现金 + 实际到账金额
```

赎回资金未到账前，不得被系统视为可立即再次申购的可用现金。

### 部分执行和非建议交易

- 部分执行必须按照用户实际金额和份额更新，不能按建议目标值更新。
- 用户可以不采纳建议；未采纳建议不能计入真实执行收益。
- 建议仍可做理论事后验证，但必须与真实执行结果分开展示。
- 用户自行进行、但不是由系统建议触发的交易，也应允许单独登记并同步持仓。

## 6. 审计与幂等要求

每次持仓变更必须保存：

- 变更前份额和现金。
- 变更后份额和现金。
- 关联建议，可为空。
- 关联交易记录。
- 实际金额、份额、净值和费用。
- 用户确认时间、平台确认时间和资金到账时间。
- 变更原因。

持仓同步接口必须提供幂等键，防止重复点击、网络重试或任务重复执行导致同一笔交易重复计入持仓。

建议在核对现有模型后，复用或增加以下数据对象：

```text
AdviceExecution
- id
- advice_snapshot_id
- idempotency_key
- status
- action
- submitted_at
- confirmed_at
- settled_at
- requested_amount
- confirmed_amount
- confirmed_units
- confirmed_nav
- actual_fee
- platform_order_ref
- user_note
- created_at
- updated_at

HoldingChangeLog
- id
- holding_id
- execution_id
- before_units
- after_units
- before_cash
- after_cash
- change_reason
- created_at
```

## 7. staging 环境规则

- staging 服务使用独立目录、独立配置和独立数据库。
- staging 只绑定 `127.0.0.1`、受控内网地址，或通过 SSH 隧道访问。
- staging 不复用生产数据库写入账号。
- 本地和 staging 使用合成数据或经过脱敏的最小测试数据。
- 如需验证生产数据结构，优先使用只读账号和只读查询。
- 禁止把生产数据库完整副本长期保存到本地。
- staging 部署不能中断生产前端、定时任务或数据库服务。

## 8. 仍需由模型核实的资料

后续模型先从服务器数据库、现有配置和正式平台资料中核实，无法确认时再询问用户：

1. 当前持仓平台及实际申购费折扣。
2. 赎回费率和真实持有开始日期。
3. 基金平台确认份额与赎回到账规则。
4. 当前基金正式披露的业绩比较基准。
5. 用户未来是否定期追加资金。

在资料补齐前继续采用保守规则：不自动交易、不自动启用优化参数、不使用生产数据库做测试、不把未到账赎回资金视为可用现金。
