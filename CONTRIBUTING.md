# 项目版本控制与信息安全规范

> 适用仓库：`my-funds`（fund-analyzer 引擎 + fund-advisor 服务）
> 本文是公开仓库文档，**只写规则与示例，绝不写任何真实密钥/密码/token/邮箱/IP**。

## 一、核心原则：公开仓库 ≠ 泄露个人信息

仓库已是 **public**（开源）。任何 `git push` 到 origin 的内容都会**永久**公开可见，
且 git 历史难以彻底清除（已吃过教训：曾用 filter-repo 重写历史清理泄露的 DB 密码）。

**唯一铁律：凡是要 commit/push 的东西，绝不允许包含——**

- 数据库密码、任何口令
- API key / token / access key / secret
- 邮箱地址（个人真实邮箱）
- 公网 IP、服务器主机名、内网 IP
- 真实持仓/账户数据
- 本地路径、个人姓名

## 二、日常提交防漏清单（提交前必查）

```bash
# 1. 看有没有不该提交的文件进了暂存区
git status

# 2. 万一误 add 了敏感文件, 立即从暂存区撤出(但文件仍在工作区)
git reset HEAD <file>

# 3. 修改任何涉及凭据/配置的代码后, 全库搜真实值(替换成你自己的)
git grep -rn "你的真实密码"      # 应无输出
git grep -rn "你的token"        # 应无输出
```

**提交消息**按类型区分（本项目现行风格）：

```
feat(模块): 新增功能
fix(模块): 修复 bug
docs: 仅文档
chore: 杂项/清理/依赖
refactor: 重构(不改行为)
test: 测试
```

示例：`fix(邮件): 操作建议加金额列, 补总量基准行`

## 三、敏感文件保护（已配置, 勿改动）

`.gitignore` 已忽略以下内容，**不要删除这些规则**：

```gitignore
.env
.env.*
!.env.example
*.pem
*.key
secrets/
*.log
frontend/dist/
frontend/node_modules/
.venv/
```

- **`.env`**：本地真实配置，绝不提交。提交的是 `.env.example`（全占位符）。
- **`.env.example`**：只能有 `your_xxx` / `example.com` 这类占位符，不能有真实值。

## 四、分支与发布约定

| 写法 | 分支 | 用途 |
|------|------|------|
| 主分支 | `main` | 可发布、可运行的稳定代码 |
| 功能分支 | `feat/xxx` | 新功能开发 |
| 修复分支 | `fix/xxx` | bug 修复 |
| 发布标签 | `vX.Y.Z` | 语义化版本, 如 v7.2.0 |

- 日常开发直接在 `main` 小步提交（本项目单人维护, 无需复杂分支流）。
- 有较大功能/里程碑时打 tag：`git tag -a v7.2.0 -m "..." && git push origin v7.2.0`。
- 版本变更同步更新 `fund-advisor/CHANGELOG.md`。

## 五、改密/换密钥时的同步流程（重要）

凡是改了数据库密码、API key、或新增密钥，**必须**：

1. 更新本地 `.env`。
2. **严禁**把新值写进任何会提交的文件（.env.example、config 默认值、文档）。
3. 若代码里曾硬编码过密钥（如 config.py 默认值），一律改成从 `.env` 读取、默认留空。
4. 同步更新所有引用该密钥的运行配置（docker-compose / 服务环境变量）。
5. 重启受影响服务并验证。

## 六、开源项目常见信息安全补充

- 收到可疑 PR（尤其改动依赖、加后门、读 `.env` 的）先审查再合。
- 不要主动在 issues/discussions 里贴本地日志、错误栈里的路径/密钥。
- 上传截图（如 README 的 `docs/screenshots/`）前，确认界面**不含真实持仓数字、邮箱、服务器信息**；敏感处打码。
- 如需"匿名/干净示例数据"，用虚构值（如金额 100、占位基金代码 000000）。

---

_备注：本项目的 `.env`、真实凭据只存在于服务器本地与 `memory/`（不在本仓库 git 跟踪范围内）。_
