# my-funds 开发与运维手册

## 1. 日常开发

1. 在 `E:\myfund11111` 检查 Git 状态并阅读相关架构文档。
2. 首次使用时执行 `E:\myfund11111\ops\Initialize-LocalDevelopment.ps1`。
3. 使用虚构数据、合成 MySQL 或仓库测试夹具开发，不导入生产数据。
4. 修改后执行 `ops/Invoke-LocalChecks.ps1 -RunPythonTests -BuildFrontend`。
5. 检查差异后提交到 GitHub，记录待部署提交 SHA。
6. 未明确要求部署时，不对服务器运行状态做变更。

## 2. 发布前检查

- 本地工作区没有意外文件或秘密。
- 目标提交已推送到 GitHub。
- 服务器磁盘空间、服务状态和仓库状态正常。
- 服务器仓库没有未提交改动。
- 数据库迁移已评估向前与回滚兼容性。
- 服务器端备份目录、容量和恢复命令已验证。
- 已记录当前线上提交 SHA，作为回滚目标。

## 3. 标准发布

1. 运行 `E:\myfund11111\ops\Test-ServerConnection.ps1` 做只读预检。
2. 在服务器创建数据库与关键配置备份，备份不下载到本地。
3. 拉取目标提交并核对 SHA。
4. 按服务器实际方式构建前端和后端。
5. 执行经确认的数据库迁移。
6. 重启或滚动更新服务。
7. 验证健康接口、首页、静态资源、API 和关键业务流程。
8. 记录发布时间、提交 SHA、迁移版本和检查结果。

## 4. 数据排查

- 优先查询表结构、行数、日期范围、聚合值和异常数量。
- 必须查看样本时，只返回脱敏后的最少字段和最少行数。
- 不输出 `.env`、连接串、API Key、邮件凭据或完整持仓。
- 复杂分析脚本在服务器运行，输出只保留结论、统计和错误定位信息。
- 数据修复先在事务或副本中验证，再确认写入生产库。

## 5. 故障处理

1. 停止继续发布和并发修改。
2. 记录故障时间、线上 SHA、服务状态和最近迁移版本。
3. 检查健康接口、容器或 systemd 状态及最小化日志。
4. 判断属于代码、配置、数据库、网络还是外部 API 故障。
5. 优先恢复服务，再进行根因修复。
6. 回滚后再次执行健康检查，并记录原因与后续措施。

## 6. 回滚原则

- 代码回滚到上一已知正常的明确 SHA。
- 数据库回滚只使用已验证方案；不可逆迁移优先采用向前修复。
- 配置回滚使用服务器端备份，不从聊天或本地临时文本恢复。
- 回滚完成后核对前端版本、后端版本、数据库版本和任务调度状态。

## 7. 首次服务器接入（已完成）

1. 用户提供 SSH 地址、用户、端口、项目绝对路径和部署方式，不提供私钥或密码。
2. 人工核对服务器 SSH 主机指纹。
3. 将非公开连接信息写入 `E:\myfund11111\ops\.env`。
4. 运行只读巡检，确认操作系统、磁盘、Git、Docker/systemd 和项目状态。
5. 已完成服务器项目、服务、端口和数据库进程方式核对；真实数据仍留在服务器。
6. 已将服务器未提交热修保存为服务器端补丁备份，正式发布前必须先审查。
7. 首次正式发布前仍需完成服务器端备份与恢复演练。

## 8. 当前发布阻塞

- 服务器工作区仍存在未提交热修，未完成审查前禁止部署新提交。
- 服务器未发现可直接调用的 mysqldump 客户端，数据库备份自动化尚未通过验证。
- 当前前端服务仍是 Vite 开发服务器，正式公网发布应先切换到构建产物和 Nginx。
- 生产数据库精确版本和连接方式尚未确认，运行中旧 `mysqld` 映像缺少 `libssl.so.1.1`，维护验证前禁止重启。
- 生产后端 Python 3.12.13 的业务依赖来源无法由标准包元数据复现，需先建立可重复部署方式。
