# Luna 开发接手入口

开始修改代码前，请按顺序完整阅读：

1. 仓库中的 `AGENTS.md` 及其适用范围内的其他指令文件。
2. `docs/development/LOCAL_DEVELOPMENT_ENVIRONMENT.md`：本地环境、合成数据库、验证命令和生产漂移风险。
3. `docs/development/P0_ACCEPTANCE_REPORT.md`：P0 验收证据、Git 风险和下一任务边界。
4. `DEVELOPMENT_PLAN_PROFITABILITY.md`：完整问题审计、开发阶段、测试、部署和回滚规格。
5. `DEVELOPMENT_DECISIONS.md`：用户已经确认的业务规则；若与主计划中的待确认内容冲突，以该文件为准。

必须遵守的当前结论：

- 生产服务器中的代码和数据库是当前事实来源，先只读核对服务器与本地差异。
- 本地只做开发，不长期保存生产数据库和隐私数据。
- 最大可接受回撤暂定为 20%。
- 允许建立不对公网开放的 staging 服务和独立测试数据库。
- 系统只提供建议，不自动下单。
- 只有用户确认实际完成或部分完成交易后，才能按真实成交信息同步持仓。
- 用户没有操作时，持仓保持原样。
- 第一批 P0 功能验收已经通过；先审查和整理现有差异，不重复重写，不调整策略参数，不直接覆盖生产代码。

当前已验证状态：

- 本地后端 Python 3.12.13 与生产主进程版本一致。
- 本地 MySQL 8.0.43 合成库健康，Alembic 从零迁移成功。
- 2026-09-01 复验：分析引擎 165 项测试、后端 5 项服务测试通过。
- 最近一次前端生产构建通过，转换 2102 个模块，仅保留入口包体积提示。
- 本地 API 健康接口与数据库连接通过，调度器和启动净值回补默认关闭。
- 当前 `HEAD=afc19e9` 且与 `origin/main` 一致；P0 和环境改动仍未形成新提交。
- 当前有 30 个已跟踪文件变化和 25 个未跟踪文件（包含 P0 验收报告），Git 发布包尚未验收。
- `ops/Invoke-LocalChecks.ps1` 会被仓库根目录旧 `.pytest_cache` 的 ACL 阻断；测试需临时使用 `-p no:cacheprovider`。
- 服务器未被本轮修改，生产发布仍被线上热修审查、数据库备份/恢复和运行时漂移阻塞。

接手后建议执行顺序：

1. 按 `docs/development/P0_ACCEPTANCE_REPORT.md` 执行提交前收口，审查当前未提交差异。
2. 把 P0、开发环境/运维、文档和其他既有改动分组，提出提交拆分方案。
3. 使用 `-p no:cacheprovider` 运行两套完整 Python 测试，复核最近净值窗口、现金仓位、零目标、动作映射和命中率分母。
4. 未获得用户确认前不提交、不推送、不部署。
5. P0 形成明确提交并复验后，才进入 P1：建议状态、手工成交确认、幂等持仓同步、费用与资金到账状态。
6. 分批处理前端依赖漏洞和构建拆包，不运行未经审查的强制升级。
7. 向用户汇报修改文件、测试结果、风险和部署建议。

涉及生产数据库结构变更、覆盖服务器代码、重启生产服务或删除历史数据前，必须先向用户说明操作内容、对象和回滚方式并获得确认。
