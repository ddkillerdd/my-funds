# Luna P0 服务器发布准备与 staging 验收执行令

> 编写日期：2026-09-02
> 本地仓库：`E:\myfund11111`
> 当前目标：服务器热修固化已经完成；生产后端自动重启诊断优先执行 `05_LUNA_BACKEND_RESTART_DIAGNOSTIC.md`，闭环后再返回 `04`
> 执行角色：Luna 负责操作；本文件限定范围、阶段、停止条件和回滚证据

> 执行状态：本文件第一、二阶段已经完成，不得按旧基线重复创建分支或重做热修。第三阶段及之后仍是有效安全门禁；当前只读诊断步骤以 `05_LUNA_BACKEND_RESTART_DIAGNOSTIC.md` 为准。

## 1. 本轮目标

本轮不是直接覆盖生产环境，而是依次完成：

1. 只读复核本地、GitHub 和服务器当前状态。
2. 审查服务器 3 个未提交热修与本地同名差异的关系。
3. 只把经过确认的服务器热修固化为独立 Git 提交，不处理其他第四组改动。
4. 验证数据库备份和恢复前置条件，不重启旧 MySQL。
5. 在服务器独立目录部署明确 Git SHA，建立不对公网开放的 staging。
6. 使用独立虚构数据库、关闭调度和外发功能，通过 SSH 隧道让用户验收。
7. 用户验收通过后，另行制定生产切换步骤；本执行令默认不切生产流量。

## 2. 当前已知基线

2026-09-02 最后一次只读核对得到：

- 本地分支：`codex/p0-server-readiness`
- 本地 `HEAD`：`5f8bc18683c807dd0ef18b3b35735a7b4cffd016`
- `origin/codex/p0-server-readiness`：与本地 `HEAD` 相同
- 已固化提交：
  - `8520c07`：开发环境与引擎桥接
  - `59f84d8`：P0 跨层正确性修复
  - `cf0289e`：文档与运维边界
  - `5f8bc18`：服务器 3 项模型调用热修
- 最近一次完整验证：分析引擎 165 项通过、后端 5 项通过、前端生产构建通过
- 本地工作区仍有 5 个未提交的第四组既有改动
- 服务器当前位于 `main` 的 `afc19e9b203141e8e604fc3f1b9f5dd438637a81`
- 服务器工作区仍有 3 个未提交文件：
  - `fund-advisor/backend/services/advisor_service.py`
  - `fund-analyzer/engine/analyzer.py`
  - `fund-analyzer/engine/llm_client.py`
- 这 3 项服务器热修已在 GitHub 的 `5f8bc18` 中固化，但服务器生产工作区尚未迁移
- OpenClaw 仍有运行进程；后端与前端当前运行，但 systemd 设置 `Restart=always`
- 当前阻塞：OpenClaw 发布窗口控制边界、数据库备份工具、生产依赖复现和隔离 staging 尚未验收

以上只作为漂移检测基线。Luna 必须重新核对，不能假定状态未变化。

## 3. 必须完整阅读

开始操作前按顺序完整阅读：

1. `E:\myfund11111\AGENTS.md`
2. `E:\myfund11111\00_LUNA_START_HERE.md`
3. `E:\myfund11111\LUNA_HANDOFF.md`
4. `E:\myfund11111\01_LUNA_P0_CLOSEOUT.md`
5. `E:\myfund11111\docs\development\P0_ACCEPTANCE_REPORT.md`
6. `E:\myfund11111\docs\operations\SYSTEM_ARCHITECTURE.md`
7. `E:\myfund11111\docs\operations\RUNBOOK.md`
8. `E:\myfund11111\ops\README.md`
9. 本文件 `E:\myfund11111\02_LUNA_SERVER_RELEASE_READINESS.md`

若文档冲突，以 `AGENTS.md`、用户最新指令和更严格的生产安全边界为准。

## 4. 全程禁止事项

- 不使用聊天中出现过的 GitHub Token，不把任何 Token 写入仓库、命令参数或日志。
- 不输出 `ops/.env`、服务器 `.env`、数据库连接串、邮件凭据、API Key、私钥或真实持仓。
- 不把服务器真实数据库、上传文件或完整日志下载到本地。
- 不使用 `git reset --hard`、`git clean`、强制推送、覆盖式同步或未经审查的 `rsync --delete`。
- 不在服务器脏工作区直接执行 `git pull`、`git switch`、`git checkout` 或覆盖文件。
- 不重启、升级、迁移或替换旧 MySQL 进程。
- 不安装来源不明的软件包，不为绕过备份门禁临时拉取不受控数据库镜像。
- 不调整策略参数、风险阈值、模型选择规则或 P0 业务语义。
- 不处理本地另外 5 个第四组文件，也不暂存 `test_position.py`、`test_screener.py`。
- 不向公网开放 staging 端口，不让 staging 使用生产数据库或生产外发凭据。
- 未达到本文件门禁时，不切换生产服务、不修改公网域名、不部署数据库迁移。

## 5. 第一阶段：本地与服务器只读复核

> 状态：已完成。以下内容保留为历史审计步骤，不再按其中的 `cf0289e` 和 8 个第四组文件基线重复执行。实时漂移检查按 `03_LUNA_OPENCLAW_COLLABORATION.md` 执行。

先用中文解释每条命令用途，再执行：

```powershell
# 核对本地分支、远程跟踪关系和全部未提交差异。
git -C 'E:\myfund11111' status --short --branch

# 核对目标提交确实已推送到 GitHub。
git -C 'E:\myfund11111' rev-parse HEAD
git -C 'E:\myfund11111' rev-parse origin/codex/p0-closeout

# 确认本地未提交文件仍严格等于既有第四组清单。
git -C 'E:\myfund11111' diff --name-status
git -C 'E:\myfund11111' diff --check

# 使用既有脚本执行服务器只读巡检，不读取服务器 .env 和数据库内容。
& 'E:\myfund11111\ops\Test-ServerConnection.ps1'
```

继续使用 `ops/.env` 和 `Test-ServerConnection.ps1` 已有的安全 SSH 参数，只读检查：

- 服务器 Git 分支、`HEAD`、远程地址和工作区状态。
- 3 个服务器热修的 `name-status`、`stat` 和 `diff --check`。
- 服务器是否出现新的未提交文件、未跟踪文件或新的提交。
- 当前生产服务的 systemd 单元名、`WorkingDirectory`、`ExecStart` 和监听端口。
- 仅记录服务引用了哪些配置文件路径，不读取配置值。
- 当前前端进程、后端进程和健康接口状态。
- 数据库进程类型、精确版本、socket/端口形态和可用备份客户端名称；不读取凭据。

### 第一停止条件

出现任一情况，立即停止并向用户报告，不进入热修收编：

- 本地 `HEAD` 或远程 P0 分支不再是 `cf0289e`。
- 本地未提交文件不再严格等于当前 8 个第四组文件。
- 服务器 `HEAD` 不再是 `afc19e9`，或服务器脏文件不再严格等于已知 3 个文件。
- SSH 主机指纹变化、远程路径变化或仓库远程地址异常。
- 服务、数据库或磁盘处于异常状态。
- 只读检查需要展示秘密才能继续。

第一阶段完成后，先向用户汇报基线和漂移结果，等待确认后再开始任何文件修改、分支创建或服务器写操作。

## 6. 第二阶段：审查并固化服务器 3 个热修

> 状态：已完成。热修已固化并推送为 `5f8bc18683c807dd0ef18b3b35735a7b4cffd016`，不得重复创建分支、重写实现或再次收编第四组文件。

该阶段只处理以下三个主题：

1. `advisor_service.py` 的 `debate/cross_val` 模型路由差异。
2. `analyzer.py` 的交叉验证模型降级与重试差异。
3. `llm_client.py` 的 JSON 模式重试、严格 JSON 提示和响应预解析差异。

### 6.1 只读差异比对

分别取得服务器和本地这 3 个文件相对 `afc19e9` 的差异，逐个差异块建立对照：

- 服务器独有。
- 本地独有。
- 两边语义相同但文本不同。
- 两边完全相同。
- 与 P0 提交发生重叠。

差异可以保存到 `%TEMP%` 下新建的明确目录供比对，但不得写入仓库，也不得包含秘密。不得把完整补丁直接贴到聊天；只汇报文件、函数、差异目的和冲突结论。

若服务器差异与本地差异不能一一解释，停止，不猜测合并。

### 6.2 分支和提交边界

获得确认后，从当前 `codex/p0-closeout` 创建：

```text
codex/p0-server-readiness
```

创建分支前再次确认暂存区为空。分支创建后：

- 仅暂存上述 3 个文件中已确认属于服务器热修的差异块。
- 其他 5 个第四组文件保持未暂存、内容不变。
- 不使用 `git add -A` 或 `git add .`。
- 若现有测试不能覆盖热修，可以新增主题明确的回归测试文件；不得顺手收编 `test_position.py` 和 `test_screener.py` 的既有改动。
- 每次暂存后运行 `git diff --cached --name-status`、`git diff --cached --check` 和 `git diff --cached --stat`。

推荐提交说明：

```text
fix(runtime): 固化服务器模型调用热修
```

### 6.3 热修验收

使用禁用 pytest 缓存的方式运行：

```powershell
& 'E:\myfund11111\fund-advisor\.venv\Scripts\python.exe' -m pytest `
  'E:\myfund11111\fund-analyzer\tests' -q -p no:cacheprovider

& 'E:\myfund11111\fund-advisor\.venv\Scripts\python.exe' -m pytest `
  'E:\myfund11111\fund-advisor\backend\tests' -q -p no:cacheprovider
```

然后在 `E:\myfund11111\fund-advisor\frontend` 运行生产构建。允许既有入口包体积提示，不允许构建错误。

提交后必须验证提交快照，而不是只验证混有未提交第四组差异的工作区。记录：

- 新提交完整 SHA。
- 3 个混合文件实际纳入的差异。
- 新增回归测试及结果。
- 仍保留的 5 个第四组文件和任何未纳入差异。

验证通过后将 `codex/p0-server-readiness` 推送到 GitHub。生产部署只允许使用该远程分支上的明确 SHA，不使用本地脏工作区。

## 7. 第三阶段：数据库备份与恢复门禁

本阶段允许只读识别和在服务器受控备份目录创建备份，但禁止重启数据库。

### 7.1 只读识别

确认并记录：

- 生产数据库精确产品和版本。
- 当前进程启动方式、systemd 单元或容器归属。
- `mysqldump`、`mariadb-dump`、`mysqlpump`、`xtrabackup` 等兼容工具是否已存在。
- 数据库容量的聚合值和服务器备份目录可用空间。
- 备份所需凭据从哪个服务器本地配置文件读取；只记录路径，不输出值。
- 目标发布 SHA 是否包含 Alembic 或其他数据库结构变化。

### 7.2 可接受的备份证据

只有同时满足以下条件才算备份门禁通过：

1. 使用与生产数据库兼容的既有备份工具。
2. 备份文件只保存在服务器受控目录，目录权限不高于 `0700`，文件权限不高于 `0600`。
3. 备份文件非空，格式检查通过，并记录文件大小、时间和校验值。
4. 不在聊天或命令输出中展示备份内容、表数据或凭据。
5. 给出明确恢复命令和恢复前置条件，但不在生产库执行恢复演练。
6. 同时保存服务器 Git 热修补丁、当前服务单元和配置文件的服务器端备份。

### 第三停止条件

若没有兼容备份客户端、凭据来源不明确、空间不足、数据库版本不确定或恢复命令无法说明，立即停止部署。不得为了通过门禁而重启旧数据库、在线升级数据库或安装未经确认的软件包。

此时只向用户提供：缺失项、风险、建议安装方案或 staging 替代方案，不碰生产服务。

## 8. 第四阶段：建立隔离 staging 供用户测试

staging 是本轮默认发布目标。它必须与生产运行目录、数据库、端口和服务单元隔离。

### 8.1 staging 约束

- 从 GitHub 获取第二阶段形成的明确发布 SHA。
- 在服务器新的、已核对的 staging/release 目录创建干净工作树；不修改现有生产脏仓库。
- 使用独立 Python 虚拟环境或可复现镜像，不复用来源不明的生产 site-packages。
- 使用独立虚构数据库，不复制生产真实持仓、上传文件或日志。
- 调度器、启动净值回补、邮件外发和任何自动任务默认关闭。
- 后端和前端只监听 `127.0.0.1` 的空闲端口。
- 防火墙和公网反向代理不新增 staging 入口。
- 用户通过 SSH 本地端口转发访问，不向公网暴露。
- 不修改生产 systemd 单元；如需服务管理，使用名称明确、可停止和可删除的 staging 单元。

### 8.2 staging 构建与启动前检查

必须确认：

- 发布目录 `git status` 干净，`HEAD` 等于发布 SHA。
- Python 版本与锁定依赖兼容，依赖安装可复现。
- 数据库迁移只作用于 staging 数据库。
- 前端生产构建通过，静态文件来自发布 SHA。
- staging 配置不含生产外发凭据，不连接生产数据库。
- 选定端口未被占用，并且只绑定回环地址。

若生产依赖无法复现，停止，不允许把生产虚拟环境直接复制进 staging 充当发布产物。

### 8.3 staging 验收

启动后至少检查：

1. 后端健康接口。
2. 前端首页和静态资源。
3. 前端到后端 API 代理。
4. P0 四组正确性场景：最近净值窗口、现金与目标权重、零目标语义、动作映射与命中率。
5. 模型路由、交叉验证降级和 JSON 重试热修。
6. 调度器、邮件和生产数据访问确实关闭。
7. 最小化 staging 日志中没有秘密、真实数据或持续报错。

为用户提供一条仅在本机执行的 SSH 隧道命令和本地访问地址。隧道命令只能引用 `ops/.env` 已登记的主机和端口，不把密钥或 Token 写进命令。

用户验收期间保留 staging，不切生产。记录 staging 服务名、目录、SHA、端口和停止命令；这些信息只在必要范围内展示。

## 9. 第五阶段：生产切换条件

本执行令默认停在 staging 验收。只有用户测试通过并再次明确确认生产切换后，才制定和执行生产发布。

生产切换前必须同时具备：

- `codex/p0-server-readiness` 的明确 GitHub SHA。
- 服务器 3 个热修已进入该 SHA，生产工作区不再是未解释的事实来源。
- 数据库与关键配置备份证据完整。
- staging 完整验收通过。
- 当前生产服务的回滚目标、热修补丁和启动方式已记录。
- 发布过程不依赖覆盖脏工作区。
- 如无数据库结构变化，明确记录“不执行迁移”；如有变化，迁移和回滚方案已经单独验收。

生产切换必须使用干净发布目录和明确 SHA。任何健康检查失败时立即停止扩大变更，恢复到发布前服务目录和配置，并重新验证健康接口、首页和关键 API。

## 10. Luna 阶段性汇报格式

### 已完成的历史第一阶段（不再执行）

- 当时的本地分支、HEAD、远程 SHA 和 8 个未提交文件。
- 服务器 HEAD、3 个脏文件、服务与数据库基础状态。
- 是否出现漂移。
- 3 个热修与本地差异的对照结论。
- 是否建议进入第二阶段。
- 明确说明尚未修改服务器。

### 第二阶段后

- 新分支和提交 SHA。
- 纳入的热修差异与新增测试。
- 两套 Python 测试和前端构建结果。
- 剩余第四组差异。
- GitHub 推送状态。
- 明确说明尚未部署生产。

### staging 完成后

- 发布 SHA、staging 目录和服务名。
- 独立数据库、关闭外发和回环绑定证据。
- 健康检查与 P0/热修验收结果。
- 用户本地 SSH 隧道命令和访问地址。
- 停止与清理 staging 的命令。
- 生产发布门禁是否全部满足。
- 明确说明生产流量尚未切换。

## 11. 历史启动指令状态

以下旧入口已经停用，避免 Luna 按完成前基线重复执行。当前启动指令以 `05_LUNA_BACKEND_RESTART_DIAGNOSTIC.md` 第 10 节为准。

```text
不要使用本节启动新任务；转读 05_LUNA_BACKEND_RESTART_DIAGNOSTIC.md。
```
